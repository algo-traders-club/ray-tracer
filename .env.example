# Ray Tracer Educational Template Configuration
# Copy this file to .env and fill in your actual values

# ==========================================
# REQUIRED CONFIGURATION
# ==========================================

# Your Solana wallet private key (base58 encoded)
# ⚠️  SECURITY WARNING: Never commit your actual private key to version control
# 
# To generate a new keypair:
# 1. Install Solana CLI: sh -c "$(curl -sSfL https://release.solana.com/v1.18.4/install)"
# 2. Generate keypair: solana-keygen new --outfile ~/.config/solana/ray-tracer-keypair.json
# 3. Get private key: cat ~/.config/solana/ray-tracer-keypair.json
# 4. Copy the entire array and convert to base58, or use: solana-keygen pubkey ~/.config/solana/ray-tracer-keypair.json
PRIVATE_KEY=your_base58_encoded_private_key_here

# Helius RPC URL with API key (REQUIRED)
# Get your API key from: https://helius.dev (free tier: 100k requests/month)
# Alternative: Use public RPC (slower): https://api.mainnet-beta.solana.com
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your_helius_api_key_here

# ==========================================
# TRADING PARAMETERS
# ==========================================

# Slippage tolerance for transactions (percentage)
# Lower = more precise pricing, higher chance of failure
# Higher = more flexible pricing, less chance of failure
# Recommended: 0.5% for stable conditions, 1-2% for volatile markets
# Range: 0.1 - 50.0 (values above 5% not recommended for most cases)
SLIPPAGE_TOLERANCE=0.5

# Priority fee for transaction processing (micro-lamports)
# Higher fees = faster confirmation during network congestion
# 1 lamport = 0.000000001 SOL, 1000 micro-lamports = 0.000001 SOL
# Recommended: 1000-5000 for normal conditions, 10000+ for congestion
# Range: 0 - 1000000 (1000000 = ~$0.0001 at $100 SOL)
PRIORITY_FEE_MICRO_LAMPORTS=1000

# ==========================================
# ADVANCED SETTINGS (OPTIONAL)
# ==========================================

# RPC connection commitment level
# "confirmed" = balance between speed and safety (recommended)
# "finalized" = maximum safety, slower
# "processed" = fastest, less safe
COMMITMENT_LEVEL=confirmed

# RPC connection timeout in milliseconds
# Increase if you experience timeout errors
# Default: 30 seconds
RPC_TIMEOUT_MS=30000

# Maximum retry attempts for failed operations
# Higher values = more persistent retries
# Range: 1-10
MAX_RETRIES=3

# Base delay between retries in milliseconds
# Exponential backoff will multiply this value
RETRY_BASE_DELAY_MS=1000

# Monitor update interval in milliseconds
# Lower = more frequent updates, higher RPC usage
# Higher = less frequent updates, lower RPC usage
# Range: 1000-60000 (1-60 seconds)
MONITOR_INTERVAL_MS=5000

# Maximum position size in SOL (safety limit)
# Prevents accidentally depositing large amounts
# Set to 0 to disable limit
MAX_POSITION_SIZE_SOL=1.0

# Enable test mode (uses minimal amounts for safety)
# true = uses 0.001 SOL for all operations
# false = uses actual specified amounts
TEST_MODE=false

# Log level for debugging
# "info" = normal output
# "verbose" = detailed operation logs
# "debug" = maximum detail
LOG_LEVEL=info

# ==========================================
# EXAMPLE CONFIGURATIONS
# ==========================================

# Conservative Setup (Recommended for beginners):
# SLIPPAGE_TOLERANCE=1.0
# PRIORITY_FEE_MICRO_LAMPORTS=5000
# MAX_POSITION_SIZE_SOL=0.1
# TEST_MODE=true

# Aggressive Trading Setup:
# SLIPPAGE_TOLERANCE=2.0
# PRIORITY_FEE_MICRO_LAMPORTS=10000
# MAX_RETRIES=5
# MONITOR_INTERVAL_MS=2000

# Development Setup:
# COMMITMENT_LEVEL=processed
# LOG_LEVEL=debug
# TEST_MODE=true
# MAX_POSITION_SIZE_SOL=0.01

# ==========================================
# SECURITY NOTES
# ==========================================
#
# 1. NEVER share your private key with anyone
# 2. Use a separate wallet for testing/learning
# 3. Start with small amounts (< 0.1 SOL)
# 4. Always verify pool addresses before trading
# 5. Understand impermanent loss risks
# 6. Keep this .env file secure and never commit it
#
# Current Pool Information:
# Pool ID: 5aEG1Vv4dJE5MiFqLTXrvVJPVD5g2d2FgK7vPco2KHfJ
# Token A: NECK (BByRYGw5yrSQpPXFYoy7euzwtxwkbKz8JpQwz9sgpump)
# Token B: WSOL (So11111111111111111111111111111111111111112)
#
# Verify these addresses at: https://dexscreener.com/solana/